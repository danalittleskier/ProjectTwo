<!DOCTYPE html>
<html lang="en">

<head>
    <title>tool results - Toolz</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" type="text/css" href="styles/styles.css">
</head>

<body>
<nav class="z-depth-0" id="navbar">
  <div class="nav-wrapper grey darken-3">
    <a href="#!" class="brand-logo center" id="brand">Toolz</a>
    <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    <ul class="right hide-on-med-and-down">
      <li><a href="/login" data-target="login"
          class="btn waves-effect waves-light grey darken-2">Login</a></li>
    </ul>
    <form class="hide-on-med-and-down" id="form1">
      <div class="input-field" style="max-width: 400pt;">
        <input id="search" type="search" required onkeyup="getTools(this.value,0);">
        <label class="label-icon" for="search"><i class="material-icons">search</i></label>
        <i class="material-icons">close</i>
      </div>
    </form>
  </div>
</nav>

<ul class="sidenav" id="mobile-demo">
  <li><a href="/login" data-target="login">Login</a></li>
</ul>

<div class="tools-image2">
    <div class="container">
        <div class="row">
            <div class="col s12">
                <div class="card grey darken-2">
                    <div class="card-content white-text">
                        <span class="card-title">{{tool.name}}</span>
                        <p>Tool ID #{{tool.id}}</p>
                        <p><strong>Description </strong> {{tool.description}}</p>
                        <p><strong>Price: </strong> ${{tool.price}}</p>
                        {{#if tool.rented}}
                        <button class="btn waves-effect waves-light grey darken-1">OUT
                            <i class="material-icons right">build</i>
                        </button>
                        {{else}}
                        <button class="btn waves-effect waves-light grey darken-3 rent" data-tool-id="{{tool.id}}"
                            data-owner-id="{{tool.MemberId}}" data-tool-price="{{tool.price}}">Rent
                            <i class="material-icons right">build</i>
                        </button>
                        {{/if}}
                    </div>
                    <div class="card-action">
                        <a class="white-text" href="/">← Back To Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script type="text/javascript"
  src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
{{!-- <script src="/js/index.js"></script> --}}
<script src="/js/tool.js"></script>